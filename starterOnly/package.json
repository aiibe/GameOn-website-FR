{
  "name": "gameon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass": "npx sass --watch scss/main.scss:css/style.sass.css --no-source-map --style=compressed",
    "prefix": "npx postcss --watch css/style.sass.css -o css/style.css --no-map --use autoprefixer",
    "dev": "npm-run-all --parallel sass prefix"
  },
  "browserslist": [
    "last 4 version"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.3.1",
    "npm-run-all": "^4.1.5",
    "postcss": "^8.3.5",
    "postcss-cli": "^8.3.1",
    "sass": "^1.35.1"
  }
}